services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - sqlite-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/scout-bingo.db
      - NODE_ENV=production
    env_file: .env

volumes:
  sqlite-data:
